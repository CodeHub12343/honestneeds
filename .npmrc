# Optimize npm for production builds
# Skip optional dependencies to speed up installation
production=true
legacy-peer-deps=true

# Use exact versions to avoid resolution delays
save-exact=false

# Limit the number of parallel requests
fetch-timeout=60000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-retries=5
